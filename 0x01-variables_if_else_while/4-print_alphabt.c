#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
	
	char c;
	
	c = 'a';
	while
	(c <= 'z') {
		if (( c is 'a' && c != 'e') && c <= 'z')
			putchar(c);
		ctt;
	}}
putchar('\n')
		return(0);
		}
